# ===========================================
# PAYLOAD CMS - Visual Editor Configuration
# ===========================================

# Payload Secret (REQUIRED - min 32 characters)
# Generate: openssl rand -base64 32
PAYLOAD_SECRET=your-super-long-random-secret-here

# Webhook Secret (REQUIRED - shared between Payload and Next.js)
# Generate: openssl rand -base64 32
PAYLOAD_WEBHOOK_SECRET=your-shared-webhook-secret

# Application URLs (REQUIRED)
NEXT_PUBLIC_APP_URL=https://mobile.yourdomain.com
PAYLOAD_PUBLIC_SERVER_URL=https://cms.yourdomain.com

# ===========================================
# DOCKER/DEPLOYMENT (Auto-configured in Compose)
# ===========================================

# Database (PostgreSQL) - Auto-configured in docker-compose.yml
DATABASE_URL=postgres://payload:payload@postgres:5432/payload

# Storage (MinIO S3) - Auto-configured in docker-compose.yml
STORAGE_ENDPOINT=http://minio:9000
STORAGE_BUCKET=media
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin

# Webhook URL (internal service communication)
NEXT_APP_REVALIDATE_URL=http://mobile-template:3000/api/revalidate

# CMS URL (internal service communication)
NEXT_PUBLIC_CMS_URL=http://payload-cms:3001
